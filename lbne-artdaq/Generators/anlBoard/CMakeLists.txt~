#link_directories("${CMAKE_CURRENT_SOURCE_DIR}/lib")

art_make(
        LIB_LIBRARIES
	/data/lbnedaq/ssp/lbne-artdaq/lbne-artdaq/Generators/anlBoard/lib/libftd2xx.so
        ${ART_UTILITIES}
        ${FHICLCPP}
        ${CETLIB}
        ${MF_MESSAGELOGGER}
        ${MF_UTILITIES}
        ${Boost_SYSTEM_LIBRARY}
        -lpthread
        )
