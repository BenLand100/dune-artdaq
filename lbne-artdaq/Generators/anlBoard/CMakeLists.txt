art_make(
        LIB_LIBRARIES
	${CMAKE_CURRENT_SOURCE_DIR}/lib/libftd2xx.so
	${CANVAS_UTILITIES}
        ${FHICLCPP}
        ${CETLIB}
        ${Boost_SYSTEM_LIBRARY}
 	lbne-artdaq_DAQLogger
        -lpthread
        )
